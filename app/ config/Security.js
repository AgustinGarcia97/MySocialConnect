export const RESULTS = {
    UNAVAILABLE: "unavailable", // no está disponible
    BLOCKED: "blocked", // dijo que no y NO se puede volver a preguntar
    DENIED: "denied", // no se preguntó o dijo que no pero se puede volver a preguntar
    GRANTED: "granted", // dio permisos
    LIMITED: "limited" // permisos parciales
};
